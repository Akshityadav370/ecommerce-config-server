# inventory-service.yml

server:
  servlet:
    context-path: /api/v1
  port: 9011

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/inventoryDB
    username: akshityadav
    password: root1234

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: always
      continue-on-error: true
      # data-locations: classpath:data.sql   # Keep commented if you have data.sql file
