# inventory-service.yml  (Clean version - push this)

server:
  port: 9011

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/inventoryDB
    username: akshityadav
    password: root1234              # Change this to your actual password if needed

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: always
      # data-locations: classpath:data.sql   # uncomment only if you have data.sql

# You can add more properties here (e.g. logging, etc.)
